// 1. Variable Let
let fullName = `Usep Syaikal Arifin`
let firstName = `Usep`
let middleName = `Syaikal`
let lastName = `Arifin`

document.writeln(`<p>${fullName}</p>`)
document.writeln(`<p>${firstName}</p>`)
document.writeln(`<p>${middleName}</p>`)
document.writeln(`<p>${lastName}</p>`)

// 2. Variable Const
const fName = `Isinya tidak bisa di ubah lagi`

fName = `Belajar` // Error