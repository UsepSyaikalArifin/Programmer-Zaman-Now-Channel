const names = [`Usep`, `Syaikal`, `Arifin`]
const fullName = `Usep Syaikal Arifin`

for (const name of names){
    document.writeln(`<p>${name}</p>`)
}

for (const char of fullName){
    document.writeln(`<p>${char}</p>`)
}